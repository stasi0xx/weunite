After Submitting the form, I want to transfer the client to the page with the calendar when they can choose free slots for the call. 

### Booking Page
-create folder `app/booking`
-inside this folder create `page.tsx`
-this file should show component `BookingCalendar.tsx` which you should create in `components/booking/`

### Booking Calendar
-it should take avaiable dates from supabase